# Node.js dependencies
node_modules/
# This covers node_modules at the root if you have one,
# and also recursively covers node_modules within subdirectories (client/node_modules, server/node_modules)

# Build artifacts for the client (React app)
# Common build output folders:
client/build/
client/dist/

# Environment variables (sensitive data, don't commit!)
.env
.env.local
.env.*.local # For different environments like .env.development.local

# Logs
npm-debug.log*
yarn-debug.log*
.pnpm-debug.log*
logs/
*.log












